{
  "name": "kubesync",
  "version": "0.0.2",
  "description": "kubesync is a small command line tool that makes it easy to deploy local config files (json and yaml) to ssh-proxied kubernetes cluster.",
  "bin": {
    "kubesync": "src/index.js"
  },
  "engines": {
    "node": ">=4.2"
  },
  "main": "src/index.js",
  "dependencies": {
    "clor": "^2.0.2",
    "commander": "^2.9.0",
    "fs-promise": "^0.5.0",
    "js-yaml": "^3.6.1",
    "lodash": "^4.13.1",
    "pad": "^1.0.0",
    "request-promise": "^3.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "kubernetes",
    "sync",
    "kubesync"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/signalive/kubesync.git"
  },
  "author": "",
  "license": "ISC"
}
